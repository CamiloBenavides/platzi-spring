services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: platzi_camilo_db
      POSTGRES_USER: camilo
      POSTGRES_PASSWORD: camilo
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
